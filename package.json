{
  "config": {
    "assetPath": "assets"
  },
  "scripts": {
    "clean": "rimraf $npm_package_config_assetPath && mkdirp $npm_package_config_assetPath",
    "watch": "chokidar 'src/**' -c 'npm run build'",
    "serve": "live-server . --watch=$npm_package_config_assetPath --port=8080 --quiet",
    "build:js": "browserify src/javascripts/index.js | uglifyjs > $npm_package_config_assetPath/bundle.min.js",
    "build:css": "node-sass src/stylesheets/index.scss --include-path node_modules | autoprefixer-cli | cleancss > $npm_package_config_assetPath/bundle.min.css",
    "build": "npm-run-all clean build:*",
    "start": "npm-run-all build --parallel serve watch"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  },
  "dependencies": {
    "snackbar": "^1.0.0-alpha.3",
    "unique-random-array": "^1.0.0"
  },
  "devDependencies": {
    "autoprefixer-cli": "^1.0.0",
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "chokidar-cli": "^1.2.0",
    "clean-css": "^3.4.8",
    "live-server": "^1.0.0",
    "mkdirp": "^0.5.1",
    "node-sass": "^3.4.2",
    "npm-run-all": "^1.3.2",
    "rimraf": "^2.5.2",
    "uglifyjs": "^2.4.10"
  }
}
